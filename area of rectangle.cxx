#include<stdio.h>
int main()
{
	int length,bredth;
	printf(" what is length of rectangle \n");
	scanf("%d",& length);
	printf("what is bredth of a rectangle\n");
	scanf("%d",&bredth);
	printf("AREA OF RECTANGLE %d",length*bredth);
	return 0;
}